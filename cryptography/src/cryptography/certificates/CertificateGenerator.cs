using System.Security.Cryptography.X509Certificates;

namespace cryptography.certificates
{
    public class CertificateGenerator
    {
        public X509Certificate GenerateNewCertificate()
        {

            var certificate =  new X509Certificate();

            return certificate;
        }
    }
}